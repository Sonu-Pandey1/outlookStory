/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['demo.tagdiv.com','jnews.io'], // Add the external domain here
    },
  };
  
  export default nextConfig;
  
